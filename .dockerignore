# Ignore everything - the Dockerfile downloads ACL2 via git clone, so
# almost nothing from the build context is needed.  The one exception is
# the xdoc agent-corpus converter, which the allcerts stage COPYs in to
# run against the freshly built manual (see tools/DESIGN.md).
*
!tools/xdoc_extract.py
